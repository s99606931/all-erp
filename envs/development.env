# Docker Compose 전체 실행 환경 (.env.docker)
# 모든 서비스를 Docker Compose로 실행할 때 사용

# ============================================================
# ALL-ERP 개발 환경 변수 템플릿
# ============================================================
# 
# 📌 용도:
#   - Docker Compose 기반 개발 환경의 환경 변수 템플릿
#   - start-dev.sh 스크립트가 이 파일을 자동으로 복사
#
# 📝 사용 방법:
#   1. 자동 복사 (권장):
#      cd dev-environment
#      ./start-dev.sh
#      → 자동으로 ../envs/development.env를 ../.env로 복사
#
#   2. 수동 복사:
#      cp envs/development.env .env
#
#   3. 값 수정:
#      .env 파일을 열어 비밀번호, 시크릿 키 등을 변경
#
# ⚠️  주의사항:
#   - 이 파일(development.env)은 템플릿이므로 Git에 커밋됩니다
#   - .env 파일은 Git에 커밋하지 마세요 (.gitignore에 포함됨)
#   - 운영 환경에서는 반드시 비밀번호와 시크릿 키를 변경하세요
#   - Docker 서비스명(postgres, redis 등)을 호스트로 사용합니다
#
# ============================================================

# Common

NODE_ENV=development
TZ=Asia/Seoul

# Database (PostgreSQL) - Docker 서비스 이름
DB_HOST=postgres
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=devpassword123
DB_DATABASE=all_erp

# Redis - Docker 서비스 이름
REDIS_HOST=redis
REDIS_PORT=6379

# RabbitMQ - Docker 서비스 이름
RABBITMQ_HOST=rabbitmq
RABBITMQ_PORT=5672
RABBITMQ_USER=admin
RABBITMQ_PASSWORD=admin

# MinIO (Object Storage for Milvus) - Docker 서비스 이름
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# Auth Service
AUTH_SERVICE_PORT=3001
JWT_SECRET=super_secret_key_change_me
JWT_EXPIRATION=1h

# System Service
SYSTEM_SERVICE_PORT=3002

# Tenant Service
TENANT_SERVICE_PORT=3006

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:3000

# Docker Image Version (운영 환경용, docker-compose.prod.yml에서 사용)
VERSION=latest

