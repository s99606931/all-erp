# Task: 개발 생산성 개선 (Improve DevEx)

## 1. 개요 (Overview)
- **목표**: 복잡한 마이크로서비스 개발 환경을 쉽게 다룰 수 있도록 NPM Scripts 표준화 및 Git Hook 도입.
- **작업 유형**: 설정 파일 수정 (package.json, husky)

## 2. 상세 작업 계획 (Detailed Plan)

### 2.1 📜 NPM Scripts 표준화
`package.json`의 `scripts` 섹션을 업데이트하여 자주 쓰는 명령어를 단축시킵니다.

- **기본 명령어**:
  - `start`: `./erp start`
  - `stop`: `./erp stop`
  - `dev`: `nx serve`
  - `build`: `nx build`
  - `test`: `nx test`
- **유틸리티**:
  - `db:up`: `docker compose -f dev-environment/docker-compose.infra.yml up -d postgres`
  - `lint:fix`: `nx run-many --target=lint --fix`

### 2.2 🐶 Husky & Lint-staged 설정
커밋 시 자동으로 코드 품질을 검사하도록 설정합니다.

1. `pnpm add -D husky lint-staged` 설치.
2. `npx husky init` 실행.
3. `.husky/pre-commit`에 `npx lint-staged` 추가.
4. `package.json`에 `lint-staged` 설정 추가:
   ```json
   "lint-staged": {
     "*.{ts,tsx,js,jsx}": [
       "eslint --fix",
       "prettier --write"
     ]
   }
   ```

### 2.3 🧪 검증 (Verification)
1. `pnpm start`로 `./erp start`가 실행되는지 확인.
2. 일부 파일을 일부러 린트 오류 나게 수정 후 `git commit` 시도 시 차단되는지 확인.

## 3. 수정 예정 파일 (Files to Modify)
- `[MODIFY] package.json`
- `[NEW] .husky/pre-commit`

## 4. 완료 조건 (Definition of Done)
- [ ] `pnpm start`, `pnpm test` 등 단축 명령어 작동 확인.
- [ ] 커밋 시 린트 검사 자동 수행 확인.
