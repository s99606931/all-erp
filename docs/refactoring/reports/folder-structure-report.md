# Phase 2: 폴더 구조 점검 보고서

> **점검 일시**: 2025-12-06  
> **대상**: 전체 백엔드 마이크로서비스 (17개)

---

## 📊 요약

### 표준 폴더 구조

```
src/app/
├── api/              # Controller Layer (선택적)
├── domain/           # Service Layer (비즈니스 로직)
├── infra/            # Repository Layer (데이터 접근)
└── {기능명}/         # Feature 기반 구조 (현재 주요 방식)
    ├── *.controller.ts
    ├── *.service.ts
    ├── dto/
    └── *.repository.ts (선택적)
```

---

## 1. System 도메인 (3개 서비스)

### ✅ auth-service

```
src/app/auth/
├── dto/
├── guards/
├── strategies/
```

**평가**: **양호** - 기능별 구조, guards/strategies 분리 우수

### ✅ tenant-service

```
src/app/tenant/
└── dto/
```

**평가**: **양호** - 단순 구조, 표준 준수

### ⚠️ system-service

```
src/app/
├── common-code/
├── department/
└── events/
```

**평가**: **양호** - 다중 기능별 구조
**개선점**: dto 폴더 누락 (common-code, department에 dto가 없음)

---

## 2. HR 도메인 (3개 서비스)

### ✅ personnel-service

```
src/app/employee/
└── dto/
```

**평가**: **양호** - 표준 준수

### ⚠️ payroll-service

```
src/app/
├── listeners/
└── payroll/
```

**평가**: **양호** - 기능별 구조
**특이사항**: listeners 폴더 분리 (이벤트 처리)

### ✅ attendance-service

```
src/app/
├── attendance/
└── leave/
```

**평가**: **양호** - 다중 기능별 구조

---

## 3. Finance 도메인 (3개 서비스)

### ✅ budget-service

```
src/app/budget/
└── dto/
```

**평가**: **양호** - 표준 준수

### ✅ accounting-service

```
src/app/journal/
└── dto/
```

**평가**: **양호** - 표준 준수

### ⚠️ settlement-service

```
src/app/report/
```

**평가**: **양호**
**개선점**: dto 폴더 누락

---

## 4. General 도메인 (3개 서비스)

### ✅ asset-service

```
src/app/asset/
└── dto/
```

**평가**: **양호** - 표준 준수

### ⚠️ supply-service

```
src/app/inventory/
```

**평가**: **양호**
**개선점**: dto 폴더 누락

### ⚠️ general-affairs-service

```
src/app/vehicle/
```

**평가**: **양호**
**개선점**: dto 폴더 누락

---

## 5. Platform 도메인 (4개 서비스)

### ✅ notification-service

```
src/app/modules/notification/
```

**평가**: **양호** - modules 패턴 사용

### ✅ approval-service

```
src/app/
├── modules/approval/
└── prisma/
```

**평가**: **양호** - modules 패턴, prisma 분리

### ✅ file-service

```
src/app/
├── modules/file/
├── prisma/
└── services/
```

**평가**: **양호** - modules 패턴

### ⚠️ report-service

```
src/app/
├── report/dto/
└── report-template/dto/
```

**평가**: **양호** - 다중 기능, dto 포함

---

## 📋 개선 필요 사항

### 우선순위 높음 (수동 처리 필요)

없음 - 전체적으로 양호한 구조

### 우선순위 중간 (선택적)

1. **DTO 폴더 누락 서비스** (5개)

   - `system-service` (common-code, department)
   - `settlement-service` (report)
   - `supply-service` (inventory)
   - `general-affairs-service` (vehicle)

   👉 **조치**: DTO 사용 시 `dto/` 폴더 생성 권장

2. **네이밍 일관성**

   - `modules/` 패턴 (platform 서비스)
   - 직접 기능명 패턴 (나머지)

   👉 **판단**: 두 패턴 모두 허용 가능, 프로젝트별 일관성 유지 중

---

## 🎯 패턴 분석

### 발견된 패턴

| 패턴                | 서비스 수 | 특징                         |
| ------------------- | --------- | ---------------------------- |
| **Feature-First**   | 13개      | `app/{기능명}/` 직접 배치    |
| **Modules Pattern** | 4개       | `app/modules/{기능명}/` 패턴 |

### 권장사항

✅ **현재 구조 유지**

- 대부분의 서비스가 Feature-First 패턴을 잘 따르고 있음
- api/domain/infra 계층 분리는 필요시 선택적 적용
- DTO는 기능별 폴더 내 `dto/` 서브폴더 사용 권장

---

## 📝 결론

### 종합 평가: **B+ (양호)**

**강점**:

- ✅ 대부분의 서비스가 일관된 구조 유지
- ✅ Feature 기반 구조로 코드 탐색 용이
- ✅ 도메인별 명확한 경계

**개선 기회**:

- 5개 서비스의 DTO 폴더 구조화
- 이벤트 리스너 위치 표준화 (`listeners/` vs `events/`)

**다음 단계**:

- Phase 2 완료 후 필요시 점진적 개선
- 새로운 서비스 생성 시 표준 템플릿 사용

---

**점검자**: Gemini AI  
**다음 작업**: Phase 2 Task 2.5 - 한국어 주석 표준화
