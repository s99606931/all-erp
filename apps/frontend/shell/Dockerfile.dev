# Shell 앱 개발 환경 Dockerfile
FROM node:20-alpine

# pnpm 설치
RUN npm install -g pnpm@10.24.0

# 작업 디렉토리 설정
WORKDIR /app

# package.json과 pnpm-lock.yaml 복사
COPY package.json pnpm-lock.yaml* ./

# 의존성 설치
RUN pnpm install --frozen-lockfile

# 소스 코드 복사 (볼륨 마운트로 대체될 예정)
COPY . .

# 3000 포트 노출
EXPOSE 3000

# 개발 서버 실행
CMD ["pnpm", "dev", "--host"]
